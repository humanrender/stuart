import { render, screen } from "@testing-library/react";

import { AddressInput } from "../../components/AddressInput";

describe("AddressInput", () => {
  describe("when a user writes a valid address", () => {
    it("should geolocate it", () => {});
  });
});
